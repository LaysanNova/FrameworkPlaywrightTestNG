package pages.model;

import com.microsoft.playwright.Page;
import pages.model.BaseMenu;

public class Footer<TPage> extends BaseMenu<TPage> {
    protected Footer(Page page) {
        super(page);
    }

}
